# Known Issues

## Open
- Deep recursion can produce dense geometry that exceeds mobile rendering budgets.
- SVG path performance may degrade at high recursion depths, requiring Canvas fallback.
- Locality-mapping mode needs careful resampling to avoid aliasing artefacts.

## Resolved
- None yet.
