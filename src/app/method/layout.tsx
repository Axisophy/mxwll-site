import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Method - MXWLL',
  description: 'How we work: explanation design, structured audits, data visualisation, and illustration systems. A systematic approach to making complex ideas comprehensible.',
}

export default function MethodLayout({ children }: { children: React.ReactNode }) {
  return children
}
