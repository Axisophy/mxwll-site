import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Contact - MXWLL',
  description: 'Get in touch with MXWLL about explanation design, data visualisation, scientific illustration, or any project involving complex communication.',
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
