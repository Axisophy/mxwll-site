import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Work - MXWLL',
  description: 'Explanation design, data visualisation, interactive systems, and scientific illustration. Selected projects from MXWLL.',
}

export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return children
}
